import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

export function WorkerPanelView() {
    return (
        <Container maxWidth="xl" sx={{ py: 3 }}>
            <Typography variant="h4">Pechat Paneli</Typography>
        </Container>
    );
}
